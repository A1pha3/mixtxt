# 2026-08-19 工作日志

## mixtxt-zola Cloudflare 部署审查与文档化

- 实测定位自定义域名 522 根因：`text-matrix.pages.dev` 返回 200（站点部署成功），`www.txtmix.com` 返回 522（裸 CNAME 未在 Pages Custom domains 登记；Pages 只服务登记过的主机，且需为该主机单独签发 TLS 证书）。
- 部署前已修的仓库级坑（均已落入仓库）：Tera 不支持 `{% macro %}`/`{% import %}`（删 macros.html、内联循环）；`static/_headers` CSP 补 `'wasm-unsafe-eval'`（Pagefind WASM 搜索线上生效）；`scripts/build.sh` 的 `npx pagefind` 补 `-y`；doc 04 §2.11 的 Root directory 由 `/` 改为 `mixtxt-zola`。
- 新增部署权威文档：`docs/product/05-CloudflarePages部署运维手册.md`（流程/自定义域名 522 根因/排查表/上线清单），并在 `docs/product/README.md` 索引登记。
- 真实部署项目名 `text-matrix`（pages.dev），域名 `www.txtmix.com`（注意与项目名 mixtxt 拼写不同）。
