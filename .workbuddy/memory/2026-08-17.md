# 2026-08-17 工作日志

## 新增 Zola 详细设计文档（场景 A 静态站 Rust 平替）
- 新建 `docs/product/04-Zola方案详细设计.md`（622 行）：Zola 专属完整设计，复用 doc 03 第一部分共享基础（指路不重复）。
- 字段映射：已发布=`draft=false`、chapterNo→`weight`、summary→`description`、自定义字段→`[extra]`、tags→`[taxonomies]`、books JSON→书目录 `_index.md`。
- 含 config.toml、Tera 模板（base/home/books/book/chapter/search）、内置 Elasticlunr 搜索、Sass、Cloudflare Pages 部署、Python+tomllib 校验脚本（无依赖）、AI 工作流、版本管理、与 Astro/Hugo 取舍表。
- 同步更新 doc 03（头部备选3 + 第六部分 + 四方案铁律）、doc 01（候选表/结论/路线/总判断）、README（结构树/场景表/阅读顺序）。
- 校验：无乱码、代码块闭合、交叉引用有效、章节编号连续。
- 关键 Zola 事实：单文件零依赖；`zola check` 链接检查；`build_search_index` 生成 `public/index.json`（Elasticlunr）；`earlier`/`later` 按 section `sort_by=weight` 给上下篇；草稿 `draft=true` 自动排除出构建/搜索/导航。

## 对抗性审查 + P0 准确性硬伤修复
- 对 4 份文档做七维（清晰/易懂/优雅/强大/准确/丰富/深刻）对抗性审查，结论是「清晰·易懂·准确基本达标；优雅·丰富·深刻·强大亏欠」，根因是「按历史文档拼接」而非「从不变量的第一性原理推导」。
- 已核实准确项：Cloudflare Pages 免费层 500 构建/月、20,000 文件、25 MiB（2026-08 多源一致，文档无误）；Zola 字段体系与官方一致。
- 修 P0 硬伤（仅准确性，用户选定范围）：
  1. Zola `zola serve` 默认**不含**草稿（官方：build/serve 都默认排除，需 `--drafts`）——doc 04 共 5 处（§2.2 流程图节点、§2.2 流动说明、§2.5 注释、§2.13 工作流、§2.16 风险）改为 `--drafts` + 注明。
  2. doc 04 §2.12 校验脚本 `if ...: pass` 空操作 → 改为真正门控：已发布章节（draft=false/缺省）若挂在 `unknown`/`private-draft` 书下则报错；并加 frontmatter 未闭合保护。
  3. 搜索 JS 假设 `index.json.documents` 为数组 → 改为兼容对象/数组两种形状（`Object.values` 兜底）。
  4. mermaid 图加可移植性提示（纯 Markdown 阅读器显示源码）。
  5. doc 01 §2.2 标题「三套主方案」→「四套」（表已含 Zola 4 行）。
- 验证：Python 校验脚本 `py_compile` 通过；grep 确认错误写法清零；代码块均闭合。
- P1/P2（结构统一、决策树、第一性原理短节、混合方案、统一 TCO、AI 生成 harness）用户暂未选，待后续。

## P1 结构优化（用户说「继续加油」，从 P1 起做）
- doc 01 顶部新增「〇、第一性原理：三个不变量与决策树」：① 静/动之刃（能否按权限门控）② 真理源（Git 文件 / DB）③ 作者界面（文件 / 网页）。把「两类场景」改写为三条不变量的**推导结果**而非凭空划分。
- 内置可执行决策树（纯文本 ASCII，可移植，无 mermaid 依赖）+ 约束反查表（带约束直接选方案）。
- doc 01 引言加「组织原则」：推荐方案给完整文档（02/03），备选在 03 内简版；Zola 因技术栈差异+用户要求详细设计单独成 04——让原先「长出来」的 Zola 独立文档变成「设计出来」的。
- doc 03 头部引用同步指向 doc 01 第〇节。
- 校验：章节编号连续（〇→一→二→三→四→五）、代码块闭合（8/42）、决策树 ASCII 完整、引用有效、无乱码。
- 仍待办的 P2（优雅/丰富/深刻/强大）：混合付费方案、统一 TCO 表、静态+付费组合架构、哲学纵深（content as code vs data）、可跑的 AI 内容生成 harness 脚本。

## P2 补进文档（用户确认「把 p2 补充到文档中」）
- **doc 01**：
  1. 不变量 2 加「哲学纵深」：为什么 Markdown+Git 正本天然利于 AI（diff 可审查/可回滚，AI 直接读写文件）——「AI 生成草稿、人决定发布」铁律成立的地基。
  2. 决策树加「只有少数章节收费 → 混合方案」分支；推导引用块、约束反查表同步加混合行。
  3. 新增「1.2 中间态：混合付费与组合架构」：静态站+付费墙（Memberful / Cloudflare Access / 极小程序）与「静态门面+付费后端组合」；明确「不变量 1 只说鉴权在服务端，没说必须全站动态」。
  4. 新增「六、统一 TCO 成本对照」：静态 ¥0 vs PocketBase ¥20-50/月 vs Payload $10-30+PG vs 小程序按量 vs SaaS 抽成。
  5. 五、总判断加中间态一行。
- **doc 03**：1.1 边界表「付费章节/VIP/订单/登录」行加注混合付费墙（指路 doc 01 §1.2）。
- **doc 04**：新增「2.18 AI 内容生成 harness」——`scripts/generate_chapters.py`（Python 3.11+ 零依赖，urllib 调 OpenAI 兼容 API；**AI 只写正文、frontmatter 由脚本统一生成**=对抗性审查得出的稳健设计）；生成 draft=true 草稿后自动跑 validate_content.py。前置条件：书 _index.md 的 copyrightStatus 必须 public-domain/authorized。
- **验证闭环**：从 md 抽取 harness+validate 脚本 py_compile 通过；dry-run 跑通；发现并修复 `[extra.seo]` 双重转义 bug（模板引号 + toml_str 双保险 → 去掉模板引号）；harness 生成的 frontmatter 构造章节文件被 validate_content.py 放行（content validation passed）。
- 最终状态：三份文档无乱码、代码块闭合（8/42/44）、编号连续（doc01 〇~六、doc04 2.14~2.18）、交叉引用有效。

## 查漏补缺（用户「继续努力，查漏补缺」）
- 扫描发现 README 与 P1/P2 后的 doc 01 不一致：README「只要付费就必须动态」与混合方案（01 §1.2）**直接矛盾**。
- 修复：
  1. **README**：标题改「三个不变量→两类场景」；场景表加「中间态：混合付费」行；关键判断改为「鉴权必须在服务端；仅少数章节可混合付费墙」；阅读顺序/结构树/04 行加 harness 提示；全局约定新增「场景 A 共享铁律」节（含付费边界）。
  2. **doc 03**：第一部分标题「三套」→「所有静态方案通用，含 04 的 Zola」；选择表付费行 + 铁律新增第 7 条「付费边界」。
  3. **doc 04**：头部引用改「第〇节与第二节」；2.17 判断与第三部分选择表的付费行加混合方案呼应。
  4. **harness 脚本**：加 `urllib.error` 错误处理（HTTP 错误友好报错后退出），提升可跑性。
- 校验：无乱码、代码块闭合（README 4/03 42/04 44）、旧表述「必须用动态方案」仅剩完整业务语境、harness py_compile 通过含错误处理。

## 查漏补缺第 2 轮（用户「继续努力，查漏补缺」）
- **系统验证**：37 个 .md 相对链接全部有效（archive 内部互相引用按 archive 目录解析，实际正确）；archive 10 个旧文档齐全；doc 03 第一部分 1.1–1.9 存在（doc 04 头部引用准确）。
- **修复 doc 04 §33 三处错误章节引用**：导航铁律（§2.9→§2.8）、草稿铁律（§2.7→doc 03 §1.7 共享铁律）、版权铁律（§2.13→§2.12 校验脚本）。
- **修复 doc 01 引言**：「推导逻辑见本节」→「见下文第〇节」。
- **修复 doc 03 1.1 边界表**「网站内置在线编辑后台」行：Hugo 不提供→「Hugo / Zola 不提供；Astro + Pages CMS / Keystatic 可提供」。
- 发现 `docs/prompt/often.md`（用户个人常用提示词备忘：角色/文档优化/autodev），不属于 product 体系，未动，仅提醒用户。
- 终检：无乱码、代码块闭合（4/8/42/42/44）、链接 0 失效。

## 对抗性审查第 2 轮（用户「从第一性原理出发，再仔细核查是否更简洁优雅/强壮/强大高效/准确」）
- **核实（多源，2026-08-17）**：
  - `taiki-e/install-action` 官方支持 zola（GitHub Releases 安装）✓；doc 04 的 Actions 写法正确。
  - Cloudflare Pages v2 构建环境已内置 Zola（默认 0.22.1），`ZOLA_VERSION` 可覆盖 ≥0.5；官方推荐预览部署用 `if [ "$CF_PAGES_BRANCH" = "main" ]... else zola build --base-url "$CF_PAGES_URL"` 分支判断。
  - **Zola 最新稳定版 0.23.2（2026-08-09）**——doc 04 原写 0.20.0 已过时。
  - **`page.earlier`/`page.later` 在 Zola 0.16 起已移除，改名 `page.lower`/`page.higher`**（官方论坛 Keats 确认）——doc 04 §2.7/§2.8 用了废弃 API，且那个"方向反了交换"注释正是用错 API 的产物。
- **修复（6 处）**：
  1. §2.7 chapter.html 模板：`page.earlier/later` → `page.lower/higher`（lower=上一章、higher=下一章）。
  2. §2.8 全文改 lower/higher + 删"方向反了交换"注释 + 加 0.16 命名说明。
  3. §2.11：`ZOLA_VERSION = 0.20.0` → `0.23.2`；构建命令改为官方分支判断写法（`CF_PAGES_BRANCH`/`CF_PAGES_URL`）。
  4. §2.12 validate：`chapterNo` 正则 `^[0-9]{3}$` → `^[0-9]{3,}$`（网络小说上千章场景）+ 报错文案同步。
  5. §2.18 harness：`datetime.now()` → `now().astimezone()`（日期带时区）；`--book` 加 `^[a-z0-9-]+$` 白名单（防路径穿越）；新增 `--parallel N` 并发生成（ThreadPoolExecutor，默认 4，上百章建议 8-16）。
- **闭环验证**：harness/validate py_compile 通过；非法 slug `../evil` 被拦截；dry-run 日期带 `+08:00`；构造 `1000.md`（chapterNo=1000）通过 validate（上限放开生效）；earlier/later 残留 0；代码块闭合 44。
- 关键 Zola 事实补充：0.16+ 用 `page.lower`/`page.higher`（earlier/later 已移除）；Pages v2 默认 Zola 0.22.1。
